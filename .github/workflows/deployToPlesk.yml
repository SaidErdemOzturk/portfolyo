name: Deploy to Plesk

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build Angular app
        run: npm run build -- --configuration production

      # Hangi klasörün oluştuğunu loglarda görmek için bu adımı ekleyelim
      - name: List build folder
        run: ls -R dist/

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: angular-dist
          # Burayı sadece dist/ yaparak içindeki her şeyi kapsama alıyoruz
          path: dist/saiderdemozturk-portfolio/

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: angular-dist
          path: dist/saiderdemozturk-portfolio/

      - name: Deploy to Plesk via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          port: ${{ secrets.FTP_PORT }}
          protocol: ftp
          # HATA ALAN /browser/ KISMINI SİLDİK
          local-dir: dist/saiderdemozturk-portfolio/
          server-dir: /httpdocs/
          # Hafızayı sıfırlamak için farklı bir isim veriyoruz
          state-name: force-sync-v1.json 
          exclude: |
            **/.git*
            **/.github*
            **/node_modules/**
